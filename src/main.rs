/// The simple main function that comes with new cargo binary crates.
/// This documentation is just placeholder.
pub fn main() {
    println!("Hello, world!");
}

/// Let's an an enum so we have plenty of doc content
pub enum Food {
    Bread,
    Rice,
    Pizza,
}
